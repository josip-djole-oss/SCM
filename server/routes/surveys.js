function registerSurveyRoutes() {
  // Survey routes still live in server.js because they depend on several local
  // survey helpers. This module is the route boundary for the next extraction.
}

module.exports = { registerSurveyRoutes };
